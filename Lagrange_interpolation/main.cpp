#include <iostream>

using namespace std;

int main()
{
    int x;
    cout<<"Enter the number of values\n";
    cout<<x;
    float a[x],b[x];
    cout<<"Enter values of x : ";
    for(int i=0;i<=x;++i){
        cin>>a[i]>>b[i];
    }
    int j;
    for(i=0;i<=x;++i)

        for(j=0;j<=x;++j){

        }

    return 0;
}
